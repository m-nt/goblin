"use strict";

module.exports = {
    goblin_config: require('./goblin.config'),
    socket_config: require('./socket.config'),
    bridge_config: require('./bridge.config'),
    database_config: require('./database.config')
}
